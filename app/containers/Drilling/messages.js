/*
 * Drilling Messages
 *
 * This contains all the text for the Drilling container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Drilling';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Drilling container!',
  },
});
